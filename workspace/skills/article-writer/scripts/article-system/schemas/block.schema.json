{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "type": { "type": "string", "enum": ["hook", "paragraph", "example", "data", "transition", "header", "list", "image"] },
      "content": { "type": "string" }
    },
    "required": ["type", "content"]
  }
}